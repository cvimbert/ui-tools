import { ToRadiansPipe } from './to-radians.pipe';

describe('ToRadiansPipe', () => {
  it('create an instance', () => {
    const pipe = new ToRadiansPipe();
    expect(pipe).toBeTruthy();
  });
});
